import { makeStyles } from "@material-ui/core/styles";
import { avatarPulse } from "../../assets/styles/main-style";

export default makeStyles({
  ...avatarPulse,
  spaceTop: {
    marginTop: "25px",
  },
  spaceBottom: {
    marginBottom: "16px",
  },
  spacedBetween: {
    margin: "2px",
  },
});
